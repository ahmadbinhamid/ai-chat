version: "2"

linters:
  default: standard
  enable:
    - unconvert
    - unparam
    - gocritic
    - errorlint
    - bodyclose
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
